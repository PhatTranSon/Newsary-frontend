export const theme = {
    primaryColor: "#e76f51",
    primaryColorLight: "#f4a261",
    primaryColorDark: "#e85d04",
    grayColorDark: "#646369",
    secondaryColor: "#3a86ff",
    secondaryColorLight: "#a8dadc",
    black: "#000",
    gray: "#eee",
    white: "#fff"
};

const sizes = {
    sm: "500px",
    lg: "1200px"
}

export const breakpoints = {
    sm: `@media only screen and (max-width:${sizes.sm})`,
    lg: `@media only screen and (max-width:${sizes.lg})`
}